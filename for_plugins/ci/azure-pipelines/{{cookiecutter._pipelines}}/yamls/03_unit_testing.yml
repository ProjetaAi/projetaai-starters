jobs:
  - job: Pytest
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - template: installing_python.yml
    - script: |
        make install-base
        make install-test
      displayName: 'Installing Testing Requirements'
    - script: |
        make check
      displayName: 'Running Pytest and Coverage'